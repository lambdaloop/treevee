[workspace]
name = "voigt-approx"
version = "0.1.0"
description = "Approximate the Voigt profile efficiently"
channels = ["conda-forge"]
platforms = ["linux-64"]

[dependencies]
python = ">=3.10,<3.12"
numpy = "*"
scipy = "*"

[tasks]
eval = "python eval.py"

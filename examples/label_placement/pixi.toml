[workspace]
name = "label-placement"
version = "0.1.0"
description = "Optimal label placement for line charts"
channels = ["conda-forge"]
platforms = ["linux-64"]

[dependencies]
python = ">=3.10,<3.12"

[tasks]
eval = "python eval.py"
